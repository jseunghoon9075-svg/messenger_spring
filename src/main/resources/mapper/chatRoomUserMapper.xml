<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.chat.mapper.ChatRoomUserMapper">
	<insert id="insertChatRoomUser" parameterType="ChatRoomUserVO">
		INSERT INTO TBL_CHAT_ROOM_USER(CHAT_ROOM_USER_ID, USER_ID, CHAT_ROOM_ID, JOINED_AT, IS_CONNECTED)
		VALUES(SEQ_CHAT_ROOM_USER.NEXTVAL, #{userId}, #{chatRoomId}, SYSDATE, 'Y')
	</insert>
	
	<select id="selectChatRoomUserByChatRoomId" parameterType="Long" resultType="ChatRoomUserVO">
		SELECT CHAT_ROOM_USER_ID, USER_ID, CHAT_ROOM_ID, JOINED_AT, IS_CONNECTED
		FROM TBL_CHAT_ROOM_USER
		WHERE CHAT_ROOM_ID = #{chatRoomId}
		AND IS_CONNECTED = 'Y'
	</select>
	
	<update id="reconnectUser">
		UPDATE TBL_CHAT_ROOM_USER
		SET IS_CONNECTED = 'Y',
			LEFT_AT = NULL
		WHERE CHAT_ROOM_ID = #{chatRoomId}
			AND USER_ID = #{userId}
	</update>

	<update id="disconnectUser">
		UPDATE TBL_CHAT_ROOM_USER
		SET IS_CONNECTED = 'N',
			LEFT_AT = SYSDATE
		WHERE CHAT_ROOM_ID = #{chatRoomId}
			AND USER_ID = #{userId}
	</update>	
	
	
</mapper>